# CDP credentials (from portal.cdp.coinbase.com)
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
CDP_NETWORK=base-sepolia

# HMAC secret for the ephemeral wallet cookie. Generate: openssl rand -hex 32
KP_SECRET=