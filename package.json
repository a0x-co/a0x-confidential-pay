{
  "name": "a0x-private-pay",
  "version": "0.1.0",
  "private": true,
  "description": "@a0x/private-pay — drop-in private USDC payments for Base. Built on CDP Non-custodial Wallets.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:demo": "pnpm --filter demo dev"
  }
}